PascalCompiler netlog;
var
f : text ;
i : integer ;

begin
//open write to data taskbar button 'type=button'.png
 assign(f, 'type.input');
 write(f);
//open ftext to link taskbar button 'type=button'.png
 for i := icon? to -png do
begin

WRITE(f, array(i:String^microsoft launcher),'-implimentation + function hide- int');
writeln(f)
end;

//close when...if do link to..repEat 0 error
close(f);
end.